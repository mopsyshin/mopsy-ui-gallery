import React, { useState, useContext } from 'react';
import { observer } from 'mobx-react-lite';
import './GorgeousPost.scss';

const GorgeousPost = props => {

  return (
    <div>
      Work in progress
    </div>
  )
}

export default observer(GorgeousPost);